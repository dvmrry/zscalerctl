.claude/settings.local.json
.claude/worktrees/
.env
.env.*
bin/
dist/
coverage.out
*.test
/zscalerctl

# Live smoke artifacts contain sanitized but confidential tenant data.
/dump/
/dump-*/
/scratch-live-smoke/
/scratch-live-smoke-*/
/scratch-live-dump/
/scratch-live-dump-*/
# Blanket scratch: agent-eval reports (`make agent-eval`), resource drafts, and
# any on-demand build artifacts. The committed docs/agentic-coverage.{md,json}
# live under docs/, NOT scratch/, so they are unaffected.
/scratch/
.playwright-mcp/
